@using Microsoft.AspNetCore.Identity;
@using BlogApp.Models
@inject SignInManager<ApplicationUser> SignInManager

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - ВашБлог</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/BlogApp.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/auth.css" />
    <link rel="stylesheet" href="~/css/mainpage.css" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <div class="topnav">
                    <!-- Логотип/название -->
                    <span class="navbar-brand" onclick="location.href='@Url.Action("Index", "Home")'" style="cursor: pointer; font-size: 1.8rem; font-weight: 700;">
                        ВашБлог
                    </span>

                    <!-- Кнопка выхода -->
                    @if (SignInManager.IsSignedIn(User))
                    {
                        <div class="menu-container">
                            <span class="menu-span" onclick="location.href='@Url.Action()'" style="cursor: pointer;">
                                Комментарии
                            </span>
                            <span class="menu-span" onclick="location.href='@Url.Action("Index", "Tag")'" style="cursor: pointer;">
                                Теги
                            </span>
                            <span class="menu-span" onclick="location.href='@Url.Action()'" style="cursor: pointer;">
                                Пользователи
                            </span>
                            <span class="menu-span" onclick="location.href='@Url.Action("Index", "Role")'" style="cursor: pointer;">
                                Роли
                            </span>
                        </div>
                        <form asp-controller="Auth" asp-action="Logout" method="post" class="ml-auto">
                            
                            <button type="submit" class="logoutButton">Выход</button>
                        </form>
                    }
                </div>
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2025 - BlogApp - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
